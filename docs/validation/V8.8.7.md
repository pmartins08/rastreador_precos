# V8.8.7 — validação

## Pré-merge

- parser reconhece Intel Arc 140V em nomenclatura portuguesa real;
- iGPU mapeada recebe classe explícita em vez do fallback genérico de integrada;
- iGPU genérica continua limitada a Prata;
- GPU dedicada mapeada mantém acesso normal a Ouro/Diamante;
- chamadas independentes a `tier_from_value()` preservam o comportamento base;
- migração de cache por título/evidência técnica tem testes dedicados;
- suite completa passou antes do merge do PR da V8.8.7.

## Pós-merge a confirmar em produção

1. Arc 140V/130V e Radeon mapeadas aparecem com `gpu_modelo` explícito quando a evidência está disponível.
2. GPUs/iGPUs genéricas não recebem Ouro/Diamante.
3. Os Ouros com GPU dedicada mapeada da V8.8.6 permanecem coerentes.
4. Se uma iGPU mapeada chegar a Ouro, o Value deve refletir a sua classe calibrada e não um fallback desconhecido.
5. Não há regressão em ntfy, persistência, matching, Price Guard ou histórico.
6. Guardar a distribuição D/O/P/B e exemplos reais para comparação futura.

Esta validação deve ser atualizada apenas com resultados observados; não usar estimativas como evidência de produção.
